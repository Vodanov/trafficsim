# TrafficSim

A small program that uses **Raylib** to simulate city traffic.

## Requirements

- Raylib
- CMake

## Compiling

Create a folder named `build` and run:

```bash
cd build/
cmake ..
make
```
